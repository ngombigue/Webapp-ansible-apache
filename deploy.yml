---
- name: "Apache container"
  hosts: prod
  become: true
  vars_files:
    - files/secrets/credentiels.yml
  tasks:
    - name: Create Apache container without starting it
      community.docker.docker_container:
        name: webapp
        image: httpd:latest
        ports:
          - "80:80"

